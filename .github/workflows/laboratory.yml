name: Tag laboratory

on:
  push:
    branches:
      - laboratory

jobs:
  ValidateCache:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v4

      - name: List
        run: ls -l
